# Placeholder Secret - populate via the deploy pipeline (GitHub Actions
# secrets -> kubectl create secret) or an external secrets operator.
# Do NOT commit real values to source control.
apiVersion: v1
kind: Secret
metadata:
  name: aks-copilot-secrets
  namespace: aks-copilot
type: Opaque
stringData:
  AZURE_OPENAI_ENDPOINT: "https://<your-aoai-resource>.openai.azure.com/"
  AZURE_OPENAI_API_KEY: "<set-via-pipeline>"
  AZURE_SEARCH_ENDPOINT: "https://<your-search-resource>.search.windows.net"
  AZURE_SEARCH_API_KEY: "<set-via-pipeline>"
